import { React, useState } from "react";
import { ScrollView, Button, Text, TextInput, View } from "react-native";

function AddToCalendarForm({ navigation }) {
  return (
    <View>
      <Text> There will be a form here</Text>
    </View>
  );
}

export default AddToCalendarForm;
